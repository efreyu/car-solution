/* Created by efreyu on 22.05.2020. */
#include <iostream>
#include <cstdlib>
#include <vector>

struct sPos {
public:
    int x;
    int y;
    sPos() { x = 0; y = 0; }
    sPos(int aX, int aY) { x = aX; y = aY; }
};


